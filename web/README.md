# Web Frontend

This directory will contain the web frontend for the Joke Couch application.

## Structure
```
web/
├── src/
│   └── (frontend code will go here)
└── Dockerfile
```

## TODO
- Add frontend framework (React, Vue, etc.)
- Connect to API
- Build Docker image
