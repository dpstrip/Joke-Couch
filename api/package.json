{
  "name": "joke-couch-api",
  "version": "0.1.0",
  "private": true,
  "description": "Joke Couch API - TypeScript API backed by CouchDB",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:api": "ts-node-dev --respawn --transpile-only src/server.ts",
    "lint": "echo \"No linter configured\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "nano": "^10.0.0",
    "swagger-ui-express": "^4.6.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.17",
    "@types/node": "^20.19.24",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
